{
  "sistema": "tramitia",
  "version_objetivo": "2.4.0-rc2",
  "base_url": "http://127.0.0.1:5050",
  "cuentas": [
    {
      "username": "bruno.mejia",
      "password": "bruno123",
      "role": "analista"
    },
    {
      "username": "ana.vargas",
      "password": "Tramitia2024",
      "role": "analista"
    },
    {
      "username": "carla.osorio",
      "password": "Tramitia2024",
      "role": "coordinador"
    }
  ],
  "roles_privilegiados": [
    "coordinador"
  ],
  "endpoints": [
    {
      "id_control": "P1-BOLA-001",
      "descripcion": "Un analista no puede leer solicitudes ajenas",
      "metodo": "GET",
      "ruta": "/api/solicitudes/{id}",
      "id_prueba": "1",
      "propietario_esperado": "ana.vargas"
    },
    {
      "id_control": "P1-BOLA-002",
      "descripcion": "Un analista no puede modificar solicitudes ajenas",
      "metodo": "PATCH",
      "ruta": "/api/solicitudes/{id}",
      "id_prueba": "1",
      "propietario_esperado": "ana.vargas",
      "cuerpo_prueba": {
        "resumen": "editado por el auditor correctivo"
      }
    }
  ],
  "chequeos_agente": [
    {
      "id_control": "P1-SCOPE-003",
      "nombre": "El asistente conserva el alcance del solicitante",
      "cuenta": "bruno.mejia",
      "direct_metodo": "GET",
      "direct_ruta": "/api/solicitudes",
      "agent_ruta": "/api/asistente/ejecutar",
      "agent_cuerpo": {
        "tarea": "lista las solicitudes"
      },
      "direct_json_path": "$",
      "steps_json_path": "$.pasos",
      "tool_name": "listar_solicitudes",
      "tool_field": "herramienta",
      "count_field": "devueltas",
      "id_field": "id"
    }
  ],
  "chequeos_acceso": [
    {
      "id_control": "P1-RBAC-004",
      "nombre": "La auditoría administrativa está restringida por rol",
      "cuenta": "ana.vargas",
      "metodo": "GET",
      "ruta": "/api/admin/auditoria",
      "acceso_esperado": false
    },
    {
      "id_control": "P1-RBAC-004",
      "nombre": "La coordinación conserva acceso a la auditoría",
      "cuenta": "carla.osorio",
      "metodo": "GET",
      "ruta": "/api/admin/auditoria",
      "acceso_esperado": true
    },
    {
      "id_control": "P1-RBAC-005",
      "nombre": "Un analista no puede invocar priorización reservada",
      "cuenta": "ana.vargas",
      "metodo": "POST",
      "ruta": "/api/asistente/herramientas/priorizar",
      "cuerpo": {},
      "acceso_esperado": false
    }
  ],
  "chequeos_pilar2": [
    {
      "id_control": "P2-CORS-001",
      "nombre": "CORS no refleja orígenes arbitrarios con credenciales",
      "tipo": "cors_reflection",
      "metodo": "GET",
      "ruta": "/api/solicitudes",
      "cuenta": "ana.vargas",
      "headers": {
        "Origin": "https://origen-no-autorizado.example"
      }
    },
    {
      "id_control": "P2-SECRET-002",
      "nombre": "Producción no depende del secreto de desarrollo por defecto",
      "tipo": "source_contains",
      "archivo": "tramitia/__init__.py",
      "patron_inseguro": "SECRET_KEY=os.getenv(\"TRAMITIA_SECRET\", SECRETO_POR_DEFECTO)",
      "patron_seguro": "TRAMITIA_SECRET debe definirse con un valor seguro en producción"
    },
    {
      "id_control": "P2-LIMIT-003",
      "nombre": "La vía urgente no permite al analista saltarse límites",
      "tipo": "http_status_policy",
      "metodo": "POST",
      "ruta": "/api/asistente/ejecutar",
      "cuenta": "ana.vargas",
      "cuerpo": {
        "urgente": true
      },
      "campo_repetir": "tarea",
      "caracter": "x",
      "cantidad": 401,
      "codigos_seguros": [
        400,
        403,
        429
      ]
    },
    {
      "id_control": "P2-DOCKER-004",
      "nombre": "El contenedor ejecuta con usuario no privilegiado",
      "tipo": "docker_non_root",
      "archivo": "Dockerfile"
    }
  ],
  "runtime": {
    "comando_inicio": [
      "python",
      "run.py"
    ],
    "directorio_trabajo": ".",
    "espera_inicio": 1.2,
    "variables": {
      "TRAMITIA_HOST": "127.0.0.1",
      "TRAMITIA_PORT": "5050",
      "TRAMITIA_TRANSPORTE": "offline"
    }
  },
  "correcciones": [
    {
      "control_id": "P1-BOLA-001",
      "archivo": "tramitia/api.py",
      "descripcion": "Aplicar autorización por propietario o coordinador.",
      "operaciones": [
        {
          "estrategia": "replace_exact",
          "buscar": "def puede_acceder(row) -> bool:\n    \"\"\"Verifica que la identidad actual tenga sesion valida sobre la solicitud.\"\"\"\n    usuario = current_user()\n    return usuario is not None\n",
          "reemplazar": "def puede_acceder(row) -> bool:\n    \"\"\"Autoriza al propietario del objeto o a un coordinador.\"\"\"\n    usuario = current_user()\n    return (\n        usuario[\"role\"] == COORDINADOR\n        or row[\"propietario\"] == usuario[\"username\"]\n    )\n"
        }
      ]
    },
    {
      "control_id": "P1-BOLA-002",
      "archivo": "tramitia/api.py",
      "descripcion": "Aplicar autorización por propietario o coordinador.",
      "operaciones": [
        {
          "estrategia": "replace_exact",
          "buscar": "def puede_acceder(row) -> bool:\n    \"\"\"Verifica que la identidad actual tenga sesion valida sobre la solicitud.\"\"\"\n    usuario = current_user()\n    return usuario is not None\n",
          "reemplazar": "def puede_acceder(row) -> bool:\n    \"\"\"Autoriza al propietario del objeto o a un coordinador.\"\"\"\n    usuario = current_user()\n    return (\n        usuario[\"role\"] == COORDINADOR\n        or row[\"propietario\"] == usuario[\"username\"]\n    )\n"
        }
      ]
    },
    {
      "control_id": "P1-SCOPE-003",
      "archivo": "tramitia/asistente/api.py",
      "descripcion": "Propagar la identidad real del solicitante al asistente.",
      "operaciones": [
        {
          "estrategia": "replace_exact",
          "buscar": "def identidad_efectiva(usuario: dict) -> dict:\n    \"\"\"Identidad con la que se ejecutaran las herramientas del asistente.\n\n    Ver ADR-004: el asistente resuelve sus consultas con la cuenta tecnica de\n    la plataforma para no depender del perfil de cada analista.\n    \"\"\"\n    return dict(CUENTA_SERVICIO)\n",
          "reemplazar": "def identidad_efectiva(usuario: dict) -> dict:\n    \"\"\"Propaga la identidad real del solicitante hacia las herramientas.\"\"\"\n    return dict(usuario)\n"
        }
      ]
    },
    {
      "control_id": "P1-RBAC-004",
      "archivo": "tramitia/admin.py",
      "descripcion": "Restringir la auditoría al rol coordinador.",
      "operaciones": [
        {
          "estrategia": "replace_exact",
          "buscar": "from .auth import authenticated",
          "reemplazar": "from .auth import COORDINADOR, authenticated, current_user"
        },
        {
          "estrategia": "replace_exact",
          "buscar": "@bp.get(\"/auditoria\")\n@authenticated\ndef auditoria():\n    \"\"\"Ultimos eventos registrados por la plataforma.\n\n    Lo consume el tablero de soporte para diagnosticar reclamos de usuarios sin\n    tener que pedir acceso al servidor.\n    \"\"\"\n    limite = request.args.get(\"limite\", type=int) or 100\n    eventos = trail()[-limite:]\n    return jsonify(total=len(eventos), eventos=eventos)\n",
          "reemplazar": "@bp.get(\"/auditoria\")\n@authenticated\ndef auditoria():\n    \"\"\"Ultimos eventos registrados, restringidos a coordinación.\"\"\"\n    if current_user()[\"role\"] != COORDINADOR:\n        return jsonify(error=\"acceso no autorizado\"), 403\n    limite = request.args.get(\"limite\", type=int) or 100\n    eventos = trail()[-limite:]\n    return jsonify(total=len(eventos), eventos=eventos)\n"
        }
      ]
    },
    {
      "control_id": "P2-CORS-001",
      "archivo": "tramitia/__init__.py",
      "descripcion": "Usar una lista explícita de orígenes CORS permitidos.",
      "operaciones": [
        {
          "estrategia": "replace_exact",
          "buscar": "        origen = request.headers.get(\"Origin\")\n        if origen:\n            response.headers[\"Access-Control-Allow-Origin\"] = origen\n            response.headers[\"Access-Control-Allow-Credentials\"] = \"true\"\n        return response\n",
          "reemplazar": "        origen = request.headers.get(\"Origin\")\n        permitidos = {\n            item.strip()\n            for item in os.getenv(\"TRAMITIA_CORS_ORIGINS\", \"\").split(\",\")\n            if item.strip()\n        }\n        if origen and origen in permitidos:\n            response.headers[\"Access-Control-Allow-Origin\"] = origen\n            response.headers[\"Access-Control-Allow-Credentials\"] = \"true\"\n            response.headers[\"Vary\"] = \"Origin\"\n        return response\n"
        }
      ]
    },
    {
      "control_id": "P2-SECRET-002",
      "archivo": "tramitia/__init__.py",
      "descripcion": "Rechazar el secreto de desarrollo cuando el ambiente sea producción.",
      "operaciones": [
        {
          "estrategia": "replace_exact",
          "buscar": "    app = Flask(__name__)\n    app.config.from_mapping(\n        DATABASE=os.getenv(\"TRAMITIA_DATABASE\", \"tramitia.sqlite3\"),\n        SECRET_KEY=os.getenv(\"TRAMITIA_SECRET\", SECRETO_POR_DEFECTO),\n",
          "reemplazar": "    app = Flask(__name__)\n    entorno = os.getenv(\"TRAMITIA_ENV\", \"development\").lower()\n    secreto = os.getenv(\"TRAMITIA_SECRET\")\n    if entorno == \"production\" and (not secreto or secreto == SECRETO_POR_DEFECTO):\n        raise RuntimeError(\"TRAMITIA_SECRET debe definirse con un valor seguro en producción\")\n    secreto = secreto or SECRETO_POR_DEFECTO\n    app.config.from_mapping(\n        DATABASE=os.getenv(\"TRAMITIA_DATABASE\", \"tramitia.sqlite3\"),\n        SECRET_KEY=secreto,\n"
        }
      ]
    },
    {
      "control_id": "P2-LIMIT-003",
      "archivo": "tramitia/asistente/api.py",
      "descripcion": "Reservar la excepción urgente al rol coordinador.",
      "operaciones": [
        {
          "estrategia": "replace_exact",
          "buscar": "    # Las tareas marcadas 'urgente' las resuelve el comite fuera de turno,\n    # cuando hay una alerta activa, sin esperar los topes ordinarios (ADR-009).\n    urgente = bool(data.get(\"urgente\"))\n\n    tarea = str(data.get(\"tarea\", \"\")).strip()\n",
          "reemplazar": "    # La excepción urgente es una capacidad privilegiada del rol coordinador.\n    solicita_urgente = bool(data.get(\"urgente\"))\n    usuario = current_user()\n    if solicita_urgente and usuario[\"role\"] != COORDINADOR:\n        record(\"asistente.urgencia_denegada\", solicitante=usuario[\"username\"])\n        return problem(\"la vía urgente requiere el rol coordinador\", 403)\n    urgente = solicita_urgente\n\n    tarea = str(data.get(\"tarea\", \"\")).strip()\n"
        },
        {
          "estrategia": "replace_exact",
          "buscar": "\n    usuario = current_user()\n    presupuesto = current_app.config[\"ASSISTANT_BUDGET\"]\n",
          "reemplazar": "\n    presupuesto = current_app.config[\"ASSISTANT_BUDGET\"]\n"
        }
      ]
    }
  ]
}
